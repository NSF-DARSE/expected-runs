# Copy this file to .env and fill in real values. .env is gitignored.
# Get credentials from Jack Davis (jackdav@udel.edu).

# --- TrackMan Data API ---
# From the TrackMan portal's "Data integration clients" page: create a dedicated
# client (one per integration; do NOT reuse or rotate the TruMedia client).
# OAuth 2.0 client_credentials grant at https://login.trackman.com/connect/token.
TRACKMAN_CLIENT_ID=
TRACKMAN_CLIENT_SECRET=
